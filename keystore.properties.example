# Copiar a keystore.properties en la raíz del proyecto (no commitear).
# Usar la misma upload key que en versiones anteriores de Play Console
# o la configurada en Play App Signing (Setup → App signing).
storeFile=/ruta/a/upload-key.jks
storePassword=CAMBIAR
keyAlias=CAMBIAR
keyPassword=CAMBIAR
